#include<stdio.h>

int main()
{
    //1. Write a program to accept three digit number and find sum of all its digits.
    int a, b, c;
    scanf("%d%d%d", &a, &b, &c);
    printf("Sum of %d, %d and %d is %d", a, b, c, a+b+c);
    return 0;
}

